meta:
  environment: node
  toolchain:
    name: npm
    version: "21"
build:
  skip: false
  artifacts:
    "*": /
  additionalCommands: "npm run build"
run:
  scriptName: .output/server/index.mjs
  persistenceMount: /data
  containerPort: "3000"
